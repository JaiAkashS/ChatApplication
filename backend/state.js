const rooms = new Map();
const socketMeta = new Map();
const typingTimers = new Map();
const readReceipts = new Map();

module.exports = {
    rooms,
    socketMeta,
    typingTimers,
    readReceipts,
};
